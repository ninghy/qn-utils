{
  "name": "@webb/utils",
  "version": "0.0.0",
  "description": "webb工具集JaveScript向",
  "author": "qearl_ning <qearl_ning@intsig.net>",
  "homepage": "",
  "license": "ISC",
  "main": "dist/index.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "publishConfig": {
    "registry": "http://npm.qixin007.com/"
  },
  "scripts": {
    "build:cjs": "tsc -b ./tsconfig.cjs.json",
    "build:es": "tsc -b ./tsconfig.esm.json",
    "build:test": "tsc -b ./tsconfig.test.json",
    "build:types": "tsc -b ./tsconfig.types.json",
    "build:umd": "rollup -c",
    "build": "npm run clean && npm-run-all build:cjs build:types build:umd",
    "clean": "rimraf lib dist es",
    "test": "jest"
  },
  "jest": {
    "extensionsToTreatAsEsm": [
      ".ts"
    ]
  }
}